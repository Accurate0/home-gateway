# yaml-language-server: $schema=../schemas/devices.schema.json
- id: fridge-trmnl
  state: enabled
  room: kitchen
  transport:
    type: trmnl
    address: "653VZN"
  watchdog: { timeout: 12h }
  roles:
    - type: trmnl
      config:
        name: Fridge TRMNL
    - type: battery
